# change as per the requirement

version: '1.0'
stage: training
file: preprocess.yml # same as current filename; do not change this
actions:
  - name: data_featurization
    enable: true
    functions:
      module_file_path: <data_science_module_file_path>
      kwargs:
        project_name: {{ project }}
        model_name: {{ model }}
        process_name: {{ model }}_preprocess